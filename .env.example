# Local testing uses the logged-in CLI, without reading its credentials.
ASTRA_PROVIDER=claude-cli
CLAUDE_MODEL=sonnet
# After the local proof, select OpenAI and supply your API key locally.
# ASTRA_PROVIDER=openai
# OPENAI_API_KEY=
ASTRA_MODEL=gpt-6-astra
PORT=8787
DATA_FILE=data/session.json
