export function drawAttachment(c,x,y,scale,content){if(!content)return;c.save();c.translate(Math.round(x),Math.round(y));c.scale(scale,scale);for(const p of content.appearance){c.fillStyle=p.color;c.fillRect(p.x,p.y,p.w,p.h)}c.restore()}
export function drawGeneratedSkill(c,x,y,content,progress){if(!content||progress<0||progress>1)return;c.save();c.translate(x,y-10);c.globalAlpha=Math.sin(progress*Math.PI);for(const effect of content.skill.effects){c.fillStyle=effect.color;for(let i=0;i<effect.count;i++){const angle=i/effect.count*Math.PI*2+progress*effect.rotation;const r=effect.radius*(effect.shape==='orbit'?1:progress);const px=Math.cos(angle)*r,py=Math.sin(angle)*r;c.fillRect(Math.round(px),Math.round(py),2,2);if(effect.shape==='ray')for(let n=1;n<5;n++)c.fillRect(Math.round(px*n/5),Math.round(py*n/5),2,2)}}c.restore()}
